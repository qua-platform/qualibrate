export type LoginData = { username: string; password: string };

export type UserInfo = {
  name: string;
  surname: string;
  email: string;
  id: number;
  username: string;
  is_admin: boolean;
};
